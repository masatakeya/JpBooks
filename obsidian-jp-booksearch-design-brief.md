# Obsidian 日本書籍検索・読書ノートプラグイン 設計ブリーフ

## 背景・目的

既存の `booksearch` プラグインはデータベースがGoogleブックスであり、日本の書籍情報（特に絶版本・専門書・古い書籍）のカバー率が不十分。日本語書籍に強いデータソースを使い、タイトル・著者名検索から読書ノートを自動生成するObsidianプラグインを自作する。

## データソース設計（フォールバック方式・2段構成）

1. **国立国会図書館サーチ（NDL Search）** — 検索の起点。タイトル・著者名でのキーワード検索を担当。認証不要。
   - 取得項目：書名、著者、出版社、出版年、ISBN
   - API形式：SRU/XML（実装時に要注意。JSON形式のレスポンスがないため、XMLパース処理が必要）
2. **openBD** — 検索結果から1件選択した後、そのISBNを使って内容紹介（あらすじ）を補完取得。認証不要。
   - 取得項目：内容紹介（あらすじ）
   - ISBNが取得できない、またはopenBD側にデータがない場合は空欄でフォールバック（処理を止めない）

書影（カバー画像）は取得しない。楽天ブックスAPIは不使用（APIキー管理不要）。

## 検索〜ノート生成フロー

1. コマンドパレット／リボンアイコンからモーダルを開く
2. タイトル・著者名を入力 → NDL Searchで検索 → 候補リストを表示（書名・著者・出版社・出版年）
3. 候補から1件選択
4. 選択書籍のISBNでopenBDに問い合わせ、内容紹介を取得（取得不可なら空欄）
5. テンプレートに沿って読書ノートを新規作成

## ノートのfrontmatter項目

- 書名
- 著者
- 出版社
- 出版年
- ISBN
- 内容紹介（あらすじ）

読了日・評価などユーザー入力項目はfrontmatterのテンプレートに空欄として用意し、ユーザーが手動で埋める想定（自動取得の対象外）。

## 除外事項（スコープ外）

- 書影（カバー画像）の取得・埋め込み
- note-writerなど既存の他スキル・プラグインとの連携
- 楽天ブックスAPIなど価格情報系データソース
- ISBN入力（バーコードスキャン等）による検索— タイトル・著者名検索のみを実装対象とする

## 技術要件

- Obsidian Plugin API（TypeScript）
- ビルド：esbuild（Obsidian公式サンプルプラグインの構成に準拠）
- 設定画面：APIキー管理は不要（NDL・openBDともに認証不要なため、SettingTabは最小限でよい）

## 実装時の注意点

- NDL SearchのレスポンスはXML（SRU形式）のため、DOMParserまたはXMLパーサーライブラリでの処理が必要
- ISBNが取得できない書籍（絶版古書等）が一定数存在する前提で、内容紹介なしでもノート作成が完了するようフォールバックを組み込むこと
- NDL Search・openBDともにレート制限の有無を実装前に公式ドキュメントで確認すること
